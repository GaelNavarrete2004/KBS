(deffacts initial-state
   (state (monkey-horizontal at-door) ;; puerta suelo
          (box-position at-window) ;; caja ventana
          (monkey-vertical on-floor) 
          (has-banana no)) ;; no tiene banana
)
